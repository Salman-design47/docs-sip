---
/**
 * Final Call-to-Action section
 */
---

<section class="final-cta-section">
  <div class="cta-content">
    <h2 class="cta-title">Ready to add privacy?</h2>
    <p class="cta-subtitle">Get started in minutes with our SDK</p>

    <div class="cta-actions">
      <a href="/getting-started/" class="btn btn-primary">
        Read the Docs
        <span class="btn-arrow">→</span>
      </a>
      <a href="https://sip-protocol.org" target="_blank" rel="noopener" class="btn btn-secondary">
        Try the Demo
        <span class="btn-arrow">↗</span>
      </a>
    </div>

    <div class="quick-install">
      <code>npm install @sip-protocol/sdk</code>
      <button class="copy-btn" onclick="navigator.clipboard.writeText('npm install @sip-protocol/sdk')">
        Copy
      </button>
    </div>
  </div>
</section>

<style>
  .final-cta-section {
    padding: 4rem 1.5rem;
    text-align: center;
    background: linear-gradient(180deg, transparent 0%, rgba(139, 92, 246, 0.08) 100%);
    border-top: 1px solid rgba(139, 92, 246, 0.1);
  }

  .cta-content {
    max-width: 600px;
    margin: 0 auto;
  }

  .cta-title {
    font-size: 2rem;
    font-weight: 700;
    margin: 0 0 0.5rem;
    color: var(--sl-color-white);
  }

  .cta-subtitle {
    font-size: 1.1rem;
    color: var(--sl-color-gray-3);
    margin: 0 0 2rem;
  }

  .cta-actions {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 1rem;
    margin-bottom: 2rem;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.875rem 1.75rem;
    font-size: 1rem;
    font-weight: 600;
    border-radius: 0.5rem;
    text-decoration: none;
    transition: all 0.2s ease;
  }

  .btn-primary {
    background: linear-gradient(135deg, #8b5cf6 0%, #7c3aed 100%);
    color: white;
    box-shadow: 0 4px 14px rgba(139, 92, 246, 0.4);
  }

  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(139, 92, 246, 0.5);
  }

  .btn-secondary {
    background: transparent;
    color: var(--sl-color-gray-2);
    border: 1px solid var(--sl-color-gray-5);
  }

  .btn-secondary:hover {
    background: rgba(255, 255, 255, 0.05);
    border-color: var(--sl-color-gray-4);
  }

  .btn-arrow {
    transition: transform 0.2s ease;
  }

  .btn:hover .btn-arrow {
    transform: translateX(2px);
  }

  .quick-install {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.75rem 1rem;
    background: rgba(0, 0, 0, 0.3);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 0.5rem;
  }

  .quick-install code {
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, monospace;
    font-size: 0.875rem;
    color: var(--sl-color-gray-2);
  }

  .copy-btn {
    padding: 0.375rem 0.75rem;
    font-size: 0.75rem;
    font-weight: 500;
    color: var(--sl-color-gray-3);
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 0.375rem;
    cursor: pointer;
    transition: all 0.2s ease;
  }

  .copy-btn:hover {
    background: rgba(139, 92, 246, 0.2);
    border-color: rgba(139, 92, 246, 0.3);
    color: #c4b5fd;
  }

  /* Light theme */
  :root[data-theme='light'] .cta-title {
    color: var(--sl-color-black);
  }

  :root[data-theme='light'] .btn-secondary {
    color: var(--sl-color-gray-4);
    border-color: var(--sl-color-gray-2);
  }

  :root[data-theme='light'] .btn-secondary:hover {
    background: rgba(0, 0, 0, 0.03);
    border-color: var(--sl-color-gray-3);
  }

  :root[data-theme='light'] .quick-install {
    background: rgba(0, 0, 0, 0.03);
    border-color: rgba(0, 0, 0, 0.1);
  }

  :root[data-theme='light'] .quick-install code {
    color: var(--sl-color-gray-4);
  }

  :root[data-theme='light'] .copy-btn {
    background: rgba(0, 0, 0, 0.03);
    border-color: rgba(0, 0, 0, 0.1);
    color: var(--sl-color-gray-4);
  }

  :root[data-theme='light'] .copy-btn:hover {
    background: rgba(124, 58, 237, 0.1);
    border-color: rgba(124, 58, 237, 0.2);
    color: #7c3aed;
  }

  @media (max-width: 600px) {
    .cta-actions {
      flex-direction: column;
    }

    .btn {
      width: 100%;
      justify-content: center;
    }

    .quick-install {
      width: 100%;
      justify-content: space-between;
    }
  }
</style>
